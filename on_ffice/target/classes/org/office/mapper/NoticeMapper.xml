<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.office.mapper.NoticeMapper">

<!-- 글 조회 로직 -->
<select id="noticeList" resultType="org.office.domain.NoticeVO">
	SELECT * FROM notice ORDER BY notice_num DESC
</select>

<!-- 글 작성 로직 -->
<insert id="write">
	INSERT INTO NOTICE(notice_num, id, btitle, bcontent, 
					name, position, dpname, importance)
	VALUES(notice_num.nextval, #{id}, #{btitle}, #{bcontent}, 
					#{name}, #{position}, #{dpname}, #{importance})
</insert>


</mapper>

